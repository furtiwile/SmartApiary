info:
  name: Suspend User
  type: http
  seq: 11

http:
  method: POST
  url: "{{smart_apiary_api_base_url}}/auth/users/{{farmer_user_id}}/suspend"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

runtime:
  scripts:
    - type: before-request
      code: |-
        bru.setGlobalEnvVar("jwt_token", bru.getGlobalEnvVar("admin_token"));
    - type: after-response
      code: |-
        console.log("DEBUG RESPONSE [SuspendUser.yml] status:", res.status, "body:", JSON.stringify(res.body));
