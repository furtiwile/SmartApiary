info:
  name: Delete User
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{smart_apiary_api_base_url}}/auth/users/{{farmer_user_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

runtime:
  scripts:
    - type: before-request
      code: |-
        bru.setGlobalEnvVar("jwt_token", bru.getGlobalEnvVar("admin_token"));
    - type: after-response
      code: |-
        console.log("DEBUG RESPONSE [DeleteUser.yml] status:", res.status, "body:", JSON.stringify(res.body));
