info:
  name: Unpair Smart Scale
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{smart_apiary_api_base_url}}/smartscales/unpair/{{hive_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

runtime:
  scripts:
    - type: after-response
      code: |-
        console.log("DEBUG RESPONSE [UnpairSmartScale.yml] status:", res.status, "body:", JSON.stringify(res.body));
    - type: before-request
      code: |-
        bru.setGlobalEnvVar("jwt_token", bru.getGlobalEnvVar("beekeeper_token"));
