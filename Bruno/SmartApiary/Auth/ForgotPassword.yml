info:
  name: Forgot Password
  type: http
  seq: 4

http:
  method: POST
  url: "{{smart_apiary_api_base_url}}/auth/forgot-password"
  body:
    type: json
    data: |+
      {
        "email": "newuser@local"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
