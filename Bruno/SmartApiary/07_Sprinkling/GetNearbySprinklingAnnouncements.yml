info:
  name: Get Nearby Sprinkling Announcements
  type: http
  seq: 6

http:
  method: GET
  url: "{{smart_apiary_api_base_url}}/sprinklingannouncements/nearby?radiusKm=10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

runtime:
  scripts:
    - type: before-request
      code: |-
        bru.setGlobalEnvVar("jwt_token", bru.getGlobalEnvVar("beekeeper_token"));
    - type: after-response
      code: |-
        console.log("DEBUG RESPONSE [GetNearbySprinklingAnnouncements.yml] status:", res.status, "body:", JSON.stringify(res.body));
