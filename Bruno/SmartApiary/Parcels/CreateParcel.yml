info:
  name: Create Parcel
  type: http
  seq: 8

http:
  method: POST
  url: "{{smart_apiary_api_base_url}}/parcels"
  body:
    type: json
    data: |+
      {
        "name": "Parcel B",
        "latitude": 45.2682,
        "longitude": 19.8321
        
      }

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
