info:
  name: SmartApiary
  type: folder
  seq: 4

request:
  auth:
    type: bearer
    token: "{{jwt_token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        console.log("DEBUG RESPONSE [folder.yml] status:", res.status, "body:", JSON.stringify(res.body));
